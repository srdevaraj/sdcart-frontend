# sdCart Frontend — environment variable template
#
# Copy this file to `.env` and fill in values for your environment.
# NEVER commit real credentials or production URLs in source code.
#
# Expo inlines EXPO_PUBLIC_* variables at build time. After changing this
# file, restart the Metro bundler (`npm start`) with a clean cache.

# Backend API base URL. The local dev default is the Spring Boot backend
# running on localhost:8080 (docker compose up in the backend directory).
EXPO_PUBLIC_API_URL=http://localhost:8080
